apiVersion: v1
kind: Service
metadata:
  name: api-gateway
  namespace: craftpilot
  labels:
    app: api-gateway
spec:
  type: LoadBalancer
  ports:
    - port: 80
      targetPort: 8070
      protocol: TCP
      name: http
  selector:
    app: api-gateway
