apiVersion: apps/v1
kind: Deployment
metadata:
  name: user-memory-service
  labels:
    app: user-memory-service
spec:
  replicas: 1
  selector:
    matchLabels:
      app: user-memory-service
  template:
    metadata:
      labels:
        app: user-memory-service
    spec:
      volumes:
        - name: gcp-credentials
          secret:
            secretName: gcp-credentials
      containers:
        - name: user-memory-service
          image: ${DOCKER_REGISTRY}/user-memory-service:${IMAGE_TAG}
          ports:
            - containerPort: 8086
          env:
            - name: SPRING_PROFILES_ACTIVE
              value: prod
            - name: SPRING_CLOUD_GCP_PROJECT_ID
              valueFrom:
                secretKeyRef:
                  name: gcp-config
                  key: project-id
            - name: GCP_CREDENTIALS_LOCATION
              value: file:/etc/gcp/credentials/gcp-credentials.json
            - name: EUREKA_SERVER_URL
              value: http://craftpilot:13579ada@eureka-server:8761/eureka/
          volumeMounts:
            - name: gcp-credentials
              mountPath: /etc/gcp/credentials
              readOnly: true
          resources:
            limits:
              memory: "512Mi"
              cpu: "500m"
            requests:
              memory: "256Mi"
              cpu: "200m"
          readinessProbe:
            httpGet:
              path: /actuator/health/readiness
              port: 8086
            initialDelaySeconds: 30
            periodSeconds: 10
          livenessProbe:
            httpGet:
              path: /actuator/health/liveness
              port: 8086
            initialDelaySeconds: 60
            periodSeconds: 20
---
apiVersion: v1
kind: Service
metadata:
  name: user-memory-service
spec:
  selector:
    app: user-memory-service
  ports:
    - port: 8086
      targetPort: 8086
  type: ClusterIP
