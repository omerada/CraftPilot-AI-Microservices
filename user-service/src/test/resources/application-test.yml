spring:
  redis:
    host: localhost
    port: 6379
  main:
    allow-bean-definition-overriding: true

firebase:
  credential:
    path: classpath:firebase-service-account-test.json

logging:
  level:
    com.craftpilot.userservice: DEBUG
    org.springframework.data.redis: DEBUG
