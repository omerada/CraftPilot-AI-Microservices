apiVersion: v1
kind: Service
metadata:
  name: user-service
  namespace: craftpilot
  labels:
    app: user-service
spec:
  type: ClusterIP
  ports:
    - port: 8060
      targetPort: 8060
      protocol: TCP
      name: http
  selector:
    app: user-service
