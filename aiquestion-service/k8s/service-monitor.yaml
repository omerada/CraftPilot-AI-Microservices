apiVersion: monitoring.coreos.com/v1
kind: ServiceMonitor
metadata:
  name: aiquestion-service-monitor
  namespace: craftpilot
  labels:
    release: prometheus
spec:
  selector:
    matchLabels:
      app: aiquestion-service
  endpoints:
    - port: http
      path: /actuator/prometheus
      interval: 15s
  namespaceSelector:
    matchNames:
      - craftpilot
