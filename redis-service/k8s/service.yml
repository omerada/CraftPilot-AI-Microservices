apiVersion: v1
kind: Service
metadata:
  name: redis-service
  namespace: craftpilot
  labels:
    app: redis-service
spec:
  type: ClusterIP
  ports:
    - port: 8085
      targetPort: 8085
      protocol: TCP
      name: http
  selector:
    app: redis-service
