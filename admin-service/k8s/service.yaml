apiVersion: v1
kind: Service
metadata:
  name: admin-service
  namespace: craftpilot
  labels:
    app: admin-service
spec:
  type: ClusterIP
  ports:
    - port: 8097
      targetPort: 8097
      protocol: TCP
      name: http
  selector:
    app: admin-service
