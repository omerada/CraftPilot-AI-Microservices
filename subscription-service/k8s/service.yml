apiVersion: v1
kind: Service
metadata:
  name: subscription-service
  namespace: craftpilot
  labels:
    app: subscription-service
spec:
  type: ClusterIP
  ports:
    - port: 8052
      targetPort: 8052
      protocol: TCP
      name: http
  selector:
    app: subscription-service
